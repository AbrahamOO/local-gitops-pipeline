replicaCount: 1
image:
  repository: ghcr.io/abrahamoo/myapp
  tag: latest
  # digest: "sha256:..."  # CI will write this when available
service:
  port: 8000
# NOTE: For a secure, reproducible setup we publish images to GHCR via CI
# and pin the image by SHA/digest in this file (CI will open a PR to update values).
