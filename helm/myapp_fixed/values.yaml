replicaCount: 1

image:
  repository: ghcr.io/abrahamoo/myapp
  tag: latest
  # CI may fill this in with a pinned digest: e.g. sha256:...
  digest: ''

service:
  type: ClusterIP
  port: 80

resources: {}
