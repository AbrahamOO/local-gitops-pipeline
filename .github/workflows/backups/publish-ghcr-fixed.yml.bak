name: publish-ghcr-fixed (backup)

# Backup of publish-ghcr-fixed.yml

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

# (content snapshot saved as backup)
